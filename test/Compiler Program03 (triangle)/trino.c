#include <stdio.h>

int main() {
    int n = 6;

    int sum = 0;

    for (int i = 1; i <= n; ++i) {
        sum += i;
    }
    
    // printf("The %dth triangle number is: %d\n", n, sum);

    return 0;
}
